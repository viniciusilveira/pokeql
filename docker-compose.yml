version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: pokeql_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # Optional: Uncomment to run the entire app in Docker
  # app:
  #   build: .
  #   ports:
  #     - "4000:4000"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   environment:
  #     - MIX_ENV=dev
  #   volumes:
  #     - .:/app
  #     - /app/deps
  #     - /app/_build
  #   command: bash -c "mix ecto.setup && mix phx.server"

volumes:
  postgres_data: